<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="apple-touch-icon" sizes="76x76" href="~/Static/icons/apple-icon.png" />
    <link rel="icon" type="image/png" href="~/Static/icons/favicon.png" />

    <title>Proyecto progra avanzada</title>

    <link rel="stylesheet" type="text/css" href="https://fonts.googleapis.com/css?family=Inter:300,400,500,600,700,900" />
    <link href="~/Static/css/nucleo-icons.css" rel="stylesheet" />
    <link href="~/Static/css/nucleo-svg.css" rel="stylesheet" />
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link href="~/Static/css/material-dashboard.css" rel="stylesheet" />



    <style>
        /* === ÍCONO DEL USUARIO (NAVBAR DERECHA) === */
        .account-icon {
            font-size: 28px; /* tamaño moderado */
            line-height: 1;
            vertical-align: middle;
        }

        .account-link {
            padding-right: 1.2rem; /* margen derecho */
        }

        /* === NAVBAR: TEXTO E ÍCONOS === */
        .navbar .nav-link {
            font-size: 16px;
            font-weight: 500;
            color: #1f2937; /* gris oscuro moderno */
        }

        .navbar .material-symbols-rounded {
            font-size: 24px; /* íconos balanceados */
            vertical-align: middle;
        }

        /* Hover con transición */
        .navbar .nav-link:hover {
            color: #0f766e;
            transition: color 0.2s ease-in-out;
        }

        /* Centrar verticalmente todos los ítems del navbar */
        .navbar-nav .nav-item {
            display: flex;
            align-items: center;
        }

        /* === FOOTER PEGADO AL FINAL === */
        .main-wrapper {
            min-height: calc(100vh - 90px); /* empuja más el footer, ajusta si lo querés más abajo */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .content-body {
            flex-grow: 1;
        }

        /* === OPCIONAL: separa un poco el footer visualmente === */
        footer.footer {
            margin-top: 40px; /* agrega un espacio superior al footer */
        }

        .navbar .nav-link.account-link {
            display: flex;
            align-items: center;
        }

        /* dropdown un poquito más compacto */
        .dropdown-menu {
            min-width: 180px;
            border-radius: .75rem;
        }




        /* === ÍCONO DEL USUARIO (NAVBAR DERECHA) === */
        .account-icon {
            font-size: 36px; /* más grande que el resto */
            line-height: 1;
            vertical-align: middle;
        }

        .account-link {
            padding-right: 1.5rem; /* espacio del borde derecho */
            display: flex;
            align-items: center;
        }

        /* === NAVBAR: TEXTO E ÍCONOS === */
        .navbar .nav-link {
            font-size: 16px;
            font-weight: 500;
            color: #1f2937; /* gris oscuro moderno */
        }

        /* íconos normales del navbar (balanceados con texto) */
        .navbar .material-symbols-rounded {
            font-size: 24px;
            vertical-align: middle;
        }

        /* hover suave en navbar */
        .navbar .nav-link:hover {
            color: #0f766e;
            transition: color 0.2s ease-in-out;
        }

        /* Centrar todos los ítems del navbar */
        .navbar-nav .nav-item {
            display: flex;
            align-items: center;
        }

        /* === FOOTER PEGADO AL FINAL === */
        .main-wrapper {
            min-height: calc(100vh - 90px); /* empuja el footer hacia abajo */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .content-body {
            flex-grow: 1;
        }

        /* Espaciado superior visual del footer */
        footer.footer {
            margin-top: 40px;
        }

        /* === DROPDOWN DEL PERFIL === */
        .dropdown-menu {
            min-width: 180px;
            border-radius: .75rem;
            font-size: 15px;
            box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
        }

            .dropdown-menu a.dropdown-item:hover {
                background-color: #f1f5f9;
            }

        <style >
        /* === ÍCONO DEL USUARIO (NAVBAR DERECHA) === */
        .account-icon {
            font-size: 36px; /* más grande que el resto */
            line-height: 1;
            vertical-align: middle;
        }

        .account-link {
            padding-right: 1.5rem; /* espacio del borde derecho */
            display: flex;
            align-items: center;
        }

        /* === NAVBAR: TEXTO E ÍCONOS === */
        .navbar .nav-link {
            font-size: 16px;
            font-weight: 500;
            color: #1f2937; /* gris oscuro moderno */
        }

        /* íconos normales del navbar (balanceados con texto) */
        .navbar .material-symbols-rounded {
            font-size: 24px;
            vertical-align: middle;
        }

        /* hover suave en navbar */
        .navbar .nav-link:hover {
            color: #0f766e;
            transition: color 0.2s ease-in-out;
        }

        /* Centrar todos los ítems del navbar */
        .navbar-nav .nav-item {
            display: flex;
            align-items: center;
        }

        /* === FOOTER PEGADO AL FINAL === */
        .main-wrapper {
            min-height: calc(100vh - 90px); /* empuja el footer hacia abajo */
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .content-body {
            flex-grow: 1;
        }

        /* Espaciado superior visual del footer */
        footer.footer {
            margin-top: 40px;
        }

        /* === DROPDOWN DEL PERFIL === */
        .dropdown-menu {
            min-width: 180px;
            border-radius: .75rem;
            font-size: 15px;
            box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
        }

            .dropdown-menu a.dropdown-item:hover {
                background-color: #f1f5f9;
            }


        /* Forzar tamaño del icono del perfil */
        .account-icon {
            font-size: 40px !important; /* fuerza el tamaño de fuente */
            line-height: 1;
            width: 40px; /* asegura el espacio físico */
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

    </style>




</head>

<body class="g-sidenav-show bg-gray-100">

    <!-- BARRA LATERAL -->
    <aside class="sidenav navbar navbar-vertical navbar-expand-xs border-radius-lg fixed-start ms-2 bg-white my-2"
           id="sidenav-main">
        <div class="sidenav-header">
            <a class="navbar-brand px-4 py-3 m-0" href="#">
                <img src="../Static/icons/logo-ct-dark.png" class="navbar-brand-img" width="26" height="26"
                     alt="main_logo">
                <span class="ms-1 text-sm text-dark">Creative Tim</span>
            </a>
        </div>
        <hr class="horizontal dark mt-0 mb-2">
        <div class="collapse navbar-collapse w-auto" id="sidenav-collapse-main">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">
                        <i class="material-symbols-rounded opacity-5">dashboard</i>
                        <span class="nav-link-text ms-1">Dashboard</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">
                        <i class="material-symbols-rounded opacity-5">notifications</i>
                        <span class="nav-link-text ms-1">Notificaciones</span>
                    </a>
                </li>
                <li class="nav-item mt-3">
                    <h6 class="ps-4 ms-2 text-uppercase text-xs text-dark font-weight-bolder opacity-5">Cuenta</h6>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">
                        <i class="material-symbols-rounded opacity-5">person</i>
                        <span class="nav-link-text ms-1">Perfil</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">
                        <i class="material-symbols-rounded opacity-5">login</i>
                        <span class="nav-link-text ms-1">Iniciar Sesión</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-dark" href="#">
                        <i class="material-symbols-rounded opacity-5">assignment</i>
                        <span class="nav-link-text ms-1">Registrarse</span>
                    </a>
                </li>
            </ul>
        </div>
    </aside>

    <!-- CONTENIDO PRINCIPAL -->
    <main class="main-content position-relative max-height-vh-100 h-100 border-radius-lg">

        <!-- NAVBAR SUPERIOR -->
        <nav class="navbar navbar-main navbar-expand-lg px-0 mx-3 shadow-none border-radius-xl" id="navbarBlur"
             data-scroll="true">
            <div class="container-fluid py-1 px-3">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb bg-transparent mb-0 pb-0 pt-1 px-0 me-sm-6 me-5">
                    </ol>
                </nav>

                <!-- Ícono de usuario alineado a la derecha -->
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item dropdown">
                        <a class="nav-link text-body px-0 account-link" href="#" id="navbarProfile"
                           role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="material-symbols-rounded account-icon">account_circle</i>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow" aria-labelledby="navbarProfile">
                            <li><a class="dropdown-item" href="#">Opción 1</a></li>
                            <li><a class="dropdown-item" href="#">Opción 2</a></li>
                            <li><a class="dropdown-item" href="#">Opción 3</a></li>
                        </ul>
                    </li>
                </ul>

            </div>
        </nav>
        <!-- FIN NAVBAR -->
        <!-- CONTENIDO Y FOOTER -->
        <div class="container-fluid py-2 main-wrapper">
            <div class="content-body">
                @RenderBody()
            </div>

            <footer class="footer py-4">
                <div class="container-fluid">
                    <div class="row align-items-center justify-content-lg-between">
                        <div class="col-lg-6 mb-lg-0 mb-4">
                            <div class="copyright text-center text-sm text-muted text-lg-start">
                                ©
                                <script>document.write(new Date().getFullYear())</script>,
                                hecho con <i class="fa fa-heart"></i> por
                                <a href="https://www.creative-tim.com" class="font-weight-bold" target="_blank">
                                    Creative Tim
                                </a>
                                para un mejor sitio web.
                            </div>
                        </div>
                    </div>
                </div>
            </footer>
        </div>
    </main>

    <!-- SCRIPTS -->
    <script src="~/Static/js/popper.min.js"></script>
    <script src="~/Static/js/bootstrap.min.js"></script>
    <script src="~/Static/js/perfect-scrollbar.min.js"></script>
    <script src="~/Static/js/smooth-scrollbar.min.js"></script>
    <script>
        var win = navigator.platform.indexOf('Win') > -1;
        if (win && document.querySelector('#sidenav-scrollbar')) {
            var options = { damping: '0.5' }
            Scrollbar.init(document.querySelector('#sidenav-scrollbar'), options);
        }
    </script>
</body>

</html>
